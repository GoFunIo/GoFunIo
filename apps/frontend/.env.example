# Backend API base URL. Leave empty when using Vite dev proxy (/auth -> localhost:3000).
# VITE_API_URL=http://localhost:3000

# Google OAuth Web Client ID (same value as GOOGLE_CLIENT_ID on backend)
VITE_GOOGLE_CLIENT_ID=replace_me.apps.googleusercontent.com
